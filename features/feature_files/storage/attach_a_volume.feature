Feature: Attach a Volume
  This allows a user to log in to the VM and
  mount the provisioned iSCSI volume on it.

  Can we attach multiple volumes per VM?

  Still need to manually format the volume after attachment